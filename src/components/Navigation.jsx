import React from "react";

// ui
import { AppBar, Typography } from "@mui/material";

const Navigation = () => {
  return (
    <AppBar sx={{ padding: 1 }}>
      <Typography variant="h4">HTT</Typography>
    </AppBar>
  );
};

export default Navigation;
